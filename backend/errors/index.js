const BadRequest = require('./BadRequest');
const NotFound = require('./NotFound');
const Unauthorized = require('./Unauthorized');
const ServerError = require('./ServerError');

module.exports = {
  BadRequest, NotFound, Unauthorized, ServerError,
};
